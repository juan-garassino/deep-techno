# Core — install these in Colab (torch and tensorflow are pre-installed on T4)
pretty-midi>=0.2.10
mido>=1.3.0
pandas>=2.2.0
matplotlib>=3.8.0
timm>=0.9.0       # Phase 2: ViT vision encoder for sheet music

# Full install (local dev, not needed on Colab)
# torch>=2.2.2
# tensorflow>=2.16.1
